---
import recursiveLatinFull from '@fontsource-variable/recursive/files/recursive-latin-full-normal.woff2';
import '@fontsource-variable/recursive/full.css';

const fonts = [recursiveLatinFull];
---

{fonts.map((font) => <link rel="preload" as="font" href={font} type="font/woff2" crossorigin />)}
