---
interface Props {
    text: string;
    href: string;
    rel?: string;
}

const { text, href, rel } = Astro.props;
---

<!-- TODO: Catppuccin colours -->
<a class="text-white underline transition-colors hover:text-blue-300" href={href} rel={rel}>{text}</a>
